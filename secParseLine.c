#include "secParseLine.h"
#include "generalUtils.h"
#include "manageMachineWord.h"

result parseLine(sWord **sWordLst){
    unsigned char isFinished = 0;
    while (!isFinished){
        switch ((*sWordLst)->status) {
            case
        }
    }
}
